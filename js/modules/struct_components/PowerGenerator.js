export class PowerGenerator {
  /**
   * @param {string} name
   * @param {number} powerOutput
   */
  constructor(name, powerOutput) {
    this.name = name;
    this.powerOutput = powerOutput;
  }
}
